name: PulseDesk Daily Digest

on:
  schedule:
    - cron: '0 4 * * *'
    - cron: '0 15 * * *'
  workflow_dispatch:
    inputs:
      edition:
        description: 'Edition to send'
        required: false
        default: 'Morning'
        type: choice
        options:
          - Morning
          - Evening

jobs:
  send-digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Set edition
        id: edition
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "edition=${{ github.event.inputs.edition }}" >> $GITHUB_OUTPUT
          else
            HOUR=$(date -u +%-H)
            if [ "$HOUR" -ge 12 ]; then
              echo "edition=Evening" >> $GITHUB_OUTPUT
            else
              echo "edition=Morning" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Send Digest
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
          SUBSCRIBERS: ${{ secrets.SUBSCRIBERS }}
        run: python main.py --now ${{ steps.edition.outputs.edition }}